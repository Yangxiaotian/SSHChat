# SSHChat 手机使用说明书（iSH）

English version: [docs/en/ish-beginner.md](docs/en/ish-beginner.md)

给完全没用过终端的人用。密钥已经配好，你只要按下面做就能进聊天室。

---

## 你需要知道的三样东西

问管理员要（或看管理员发给你的纸条/消息）：

| 项目 | 例子（以实际为准） | 你的 |
|------|-------------------|------|
| 服务器地址 | `stdlib.gicp.net` | ________ |
| SSH 端口 | `44681` | ________ |
| 你的用户名 | 例如 `yxt` | ________ |

下文用：

```text
ssh -p 端口 用户名@服务器地址
```

把三处换成你的真实信息。

---

## 一、安装 iSH

1. 在 iPhone / iPad 的 **App Store** 搜索 **iSH**，安装（图标是黑底白字的 `ish`）。
2. 打开 iSH，会看到一个类似电脑的黑底命令行界面，底部有虚拟键盘。
3. 第一次打开若提示下载 Alpine 系统文件，等它下完即可。

---

## 二、安装 SSH 客户端（只做一次）

在 iSH 里输入下面两行，每行输完按回车：

```bash
apk update
apk add openssh-client
```

看到类似 `OK:` 就说明装好了。以后不用再装。

---

## 三、登录聊天室

把下面命令里的三处换成你的真实信息，整行复制进 iSH，回车：

```bash
ssh -p 44681 你的用户名@stdlib.gicp.net
```

**第一次连接**会问类似：

```text
Are you sure you want to continue connecting (yes/no)?
```

输入 `yes` 再回车（必须打完整的 `yes`，不能只打 `y`）。

成功后你会看到类似：

```text
[OK] ...
> 
```

出现 `>` 就说明已经进聊天室了。

---

## 四、怎么聊天

| 你想做的事 | 怎么做 |
|------------|--------|
| 说话 | 直接打字，回车发送（发给当前房间） |
| 看有哪些人 | `/names` |
| 看房间列表 | `/rooms` |
| 进某个房间 | `/join 房间名` |
| 换房间 | `/switch 房间名` |
| 私聊某人 | `/msg 对方昵称 你好呀` |
| 看全部命令 | `/help` |
| 清屏 | `/clear` |
| 查单词 | `/dict hello` 或 `/dict 学习` |
| 看新闻 | `/news` |
| 进图书馆 | `/library` 或 `/lib` |
| 小游戏说明 | `/game help` |

注意：

- 命令都要以 **`/`** 开头。
- 普通文字**不要**加 `/`，否则会被当成命令。
- 手机屏小、棋盘刷屏时，可输入 `/dnd on` 开启勿扰（轮到你下棋时只会提示一行）。

---

## 五、图书馆（看书）

聊天室里自带一个图书馆，可以看服务器上的电子书（支持 epub / txt / md / pdf）。每人有自己的书签，下次打开会从上次读到的地方继续；翻页会自动保存进度。

| 你想做的事 | 怎么做 |
|------------|--------|
| 看有哪些书 | `/library` 或 `/lib` |
| 打开第 1 本 | `/lib open 1`（数字是书目里的序号） |
| 下一页 | `/lib next` |
| 上一页 | `/lib prev` |
| 跳到第 N 页 | `/lib page 10` |
| 看我的书签 | `/lib bookmarks` |
| 清掉某本书的书签 | `/lib reset 1` |
| 结束阅读 | `/lib close`（书签仍保留） |

示例流程：

```text
/lib
/lib open 1
/lib next
/lib next
/lib close
```

小提示：

- `/library` 和 `/lib` 效果一样，手机上建议用短的 `/lib`。
- 第一次打开很大的 PDF 可能要等十几秒（会提示「正在加载…」），之后再打开就快了。
- 看书时若被别人下棋刷屏打扰，可先 `/dnd on`。

---

## 六、怎么退出

任选一种：

1. 键盘：先点 **`Ctrl`**，再点 **`c`**（或 `d`）。
2. 直接关掉 iSH App（下次重新执行登录命令即可）。

---

## 七、下次怎么用（最短步骤）

1. 打开 iSH  
2. 输入登录命令（可把命令存进备忘录，每次复制粘贴）：

```bash
ssh -p 44681 你的用户名@stdlib.gicp.net
```

3. 出现 `>` 后开始聊天  

若懒得每次打，可在 iSH 里执行一次：

```bash
echo 'alias chat="ssh -p 44681 你的用户名@stdlib.gicp.net"' >> ~/.profile
source ~/.profile
```

以后只要输入：

```bash
chat
```

就能登录。

---

## 八、常见问题

### 1. `Permission denied (publickey)`

确认用户名拼写正确（一般是小写英文）。仍不行就把完整报错发给管理员。

### 2. `Connection timed out` / 一直转圈

- 手机是否连上网（蜂窝或 Wi‑Fi）。
- 端口、地址是否抄错。
- 若在公司/学校网，可能拦了非常用端口，可换手机流量再试。

### 3. `Connection refused`

端口错了，或服务器暂时没开。问管理员确认端口是否仍是 `44681`。

### 4. 中文打不出来 / 键盘怪

- iSH 底部点键盘图标，可切换系统中文键盘。
- 需要符号时，可长按字母，或用外接键盘。

### 5. 屏幕太乱、字太小

- 用 `/clear` 清屏。
- iPhone 可双指捏合缩放（部分版本支持）；或把手机横过来。
- 玩棋类时建议 `/dnd on`。

### 6. 连上了但像「死机」、没有 `>`

- 等几秒；若仍无，`Ctrl + c` 退出后重连。
- 把屏幕截图发给管理员。

---

## 附：管理员填写区（发给小白前请填好）

```text
服务器地址：stdlib.gicp.net
SSH 端口：44681
用户名：__________
备注：________________________________
```

填好用户名后，把下面这行也发给小白，让他直接复制：

```bash
ssh -p 44681 用户名@stdlib.gicp.net
```
