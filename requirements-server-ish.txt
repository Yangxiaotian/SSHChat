# Lightweight server deps for iSH / i686 musl Alpine.
# pymupdf has no usable wheel there; library.py already falls back to pypdf.
prompt_toolkit
chess>=1.10
pypdf>=4.0
ebooklib>=0.18
# cgi was removed from the stdlib in Python 3.13+; file_http_server still needs FieldStorage.
legacy-cgi>=2.6
