# SSHChat 手机 App 使用说明书

给 **SSHChat 手机客户端**（iOS / Android 原生 App）用户。不用终端、不用 iSH，打开 App 点「连接」即可。

> 若你用的是 iSH 黑窗口终端，请看 **`小白使用说明书-iSH.md`**。

---

## 你需要知道的三样东西

问管理员要（或看管理员发给你的纸条/消息）：

| 项目 | 例子（以实际为准） | 你的 |
|------|-------------------|------|
| 服务器地址 | `stdlib.gicp.net` | ________ |
| SSH 端口 | `44681` | ________ |
| 你的 Linux 用户名 | 例如 `yxt` | ________ |

另外还需要：

- **SSHChat App 安装包**（管理员发 `.ipa` / `.apk`，或 TestFlight / 内部分发链接）。
- **把你的公钥交给管理员登记**（见下文「第一次使用」）。

---

## 一、安装 App

1. 按管理员说明安装 **SSHChat**（iPhone / Android 均可）。
2. 首次打开若提示相机、麦克风、相册等权限，请允许——发图、发语音、拍照需要这些权限。

---

## 二、第一次使用（登记公钥）

1. 打开 SSHChat，在连接页会看到一长串以 `ssh-ed25519` 开头的 **公钥**。
2. 点 **「复制公钥」**，把整行发给管理员（微信 / 邮件均可）。
3. 管理员把你的公钥加到服务器后，你才能连上。
4. 填写：
   - **SSH 主机**（服务器地址）
   - **SSH 端口**（数字）
   - **Linux 用户名**
5. 点 **「连接」**。成功后会进入聊天界面，顶部显示绿色状态栏和「已连接 …」。

**小提示：**

- App 会自动保存主机、端口、用户名；下次打开直接点「连接」。
- 重装 App 后若公钥变了，需重新复制公钥给管理员；或点 **「从备份文件恢复密钥」** 选之前的备份文件（在「文件 / SSHChat」目录下）。

---

## 三、界面一览

| 位置 | 作用 |
|------|------|
| 顶栏 **A- / A+** | 调聊天字号 |
| 顶栏右侧 | 显示当前服务器 `地址:端口` |
| **发送到** | 选发给当前房间，或私聊某位在线用户 |
| 聊天区 | 显示消息；点图片可预览；私聊消息可点「私聊 · 昵称」回复 |
| 输入框 | 打字发消息或输入 `/` 命令 |
| **/** 按钮 | 快速输入斜杠，开始打命令 |
| **Tab** | 命令补全（输入 `/lib o` 再点 Tab 可补成 `/lib open `） |
| **发送** | 发出输入框内容 |
| **+** 展开面板 | 语音、拍照、文件、画板、清屏、断开 |

---

## 四、怎么聊天

| 你想做的事 | 怎么做 |
|------------|--------|
| 在当前房间说话 | 直接打字，点 **发送**（不要加 `/`） |
| 私聊某人 | 点 **发送到** → 选对方昵称 → 再打字发送 |
| 看有哪些人 | 输入 `/names` 发送 |
| 看房间列表 | `/rooms` |
| 进某个房间 | `/join 房间名` |
| 换房间 | `/switch 房间名` |
| 看全部命令 | `/help` |
| 清屏 | 点 **+ → 清屏**，或发送 `/clear` |
| 查单词 | `/dict hello` 或 `/dict 学习` |
| 看新闻 | `/news` |
| 进图书馆 | `/lib` |
| 小游戏说明 | `/game help` |
| 看书时少被打扰 | `/dnd on`（下棋刷屏会折叠；轮到你时仍有一行提示） |

注意：

- 命令都要以 **`/`** 开头；普通聊天**不要**加 `/`。
- 输入 `/` 后会出现常用命令提示，点一下可填入。

---

## 五、图书馆（看书）

聊天室自带图书馆，可看服务器上的电子书（epub / txt / md / pdf 等）。每人有自己的书签，翻页会自动保存进度。

| 你想做的事 | 怎么做 |
|------------|--------|
| 看有哪些书 | `/lib` |
| 按书名找书（未打开书时） | `/lib find 关键词`（也可用 `/lib 查找 …`） |
| 打开第 1 本 | `/lib open 1`（数字是书目里的序号） |
| 下一页 | `/lib next` |
| 上一页 | `/lib prev` |
| 跳到第 N 页 | `/lib page 10` |
| **在当前书里搜内容** | `/lib search 关键词`（也可用 `/lib 搜索 …`） |
| 看我的书签 | `/lib bookmarks` |
| 清掉某本书的书签 | `/lib reset 1` |
| 结束阅读 | `/lib close`（书签仍保留） |

### 查找 vs 搜索

- **`/lib find`**：在**全部书目**里按书名、扩展名找书（还没打开任何书时用）。
- **`/lib search`**：在**当前打开的书**里搜正文；会列出「第几页 + 片段」。若只找到 1 处，会自动跳到那一页；找到多处时，用 `/lib page 页码` 跳转。

示例：

```text
/lib find 红楼          ← 在书目里找书名含「红楼」的书
/lib open 1             ← 打开第 1 本
/lib search 林黛玉      ← 在当前书里搜「林黛玉」
/lib page 42            ← 跳到第 42 页
/lib next
/lib close
```

小提示：

- `/library` 和 `/lib` 效果一样；手机上建议用短的 `/lib`。
- 第一次打开很大的 PDF 可能要等十几秒，之后会快很多。
- 看书时可 `/dnd on`，减少下棋刷屏干扰。

---

## 六、共享画板（一起画画）

| 你想做的事 | 怎么做 |
|------------|--------|
| 当前房间一起画 | 点 **+ → 画板**，或发送 `/canvas` |
| 只和某个人画 | `/canvas 对方昵称`（对方要在线） |
| 指定房间 | `/canvas #房间名`（你必须在该房间里） |
| 关掉画板 | `/canvas close`（发起人才能关） |

**手机 App 的优势：** 创建画板后，App 会**自动弹出画板网页**并填入密钥，一般不用自己复制网址到浏览器。

步骤：

1. 点 **+ → 画板**（或发 `/canvas`）。
2. 稍等片刻，全屏画板自动打开。
3. 用手指画画；关页面点右上角 **关闭** 即可回到聊天。

---

## 七、发语音、图片、文件

展开 **+** 面板：

| 按钮 | 用法 |
|------|------|
| **语音** | **按住**说话，**松开**发送（需麦克风权限） |
| **拍照** | 点一下拍照发送；**长按**可录像发送 |
| **文件** | 从手机选文件上传（需已连接） |

收到别人发的图片/文件，在聊天区点一下即可预览或打开。

---

## 八、断开与下次使用

- 点 **+ → 断开**，确认后退出连接。
- 下次：打开 App → 检查用户名/地址/端口 → 点 **连接**。

---

## 九、常见问题

### 1. 连接失败 / Permission denied

- 用户名是否拼写正确（一般小写英文）。
- 公钥是否已交给管理员并登记成功。
- 可再点 **复制公钥** 发给管理员核对。

### 2. Connection timed out / 一直连接中

- 手机是否连上网（Wi‑Fi 或流量）。
- 主机、端口是否填错。
- 公司/学校网络可能拦截非常用端口，可换流量试。

### 3. 发语音没反应

- 到系统 **设置 → SSHChat → 麦克风** 打开权限。
- 必须 **按住** 话筒按钮，松手才发送。

### 4. 画板没弹出来

- 等几秒；若仍无，看聊天区是否有报错，截图发给管理员。
- 也可手动发 `/canvas` 再试。

### 5. 屏幕太乱、字太小

- 顶栏 **A+ / A-** 调字号。
- **+ → 清屏** 或 `/clear`。
- 玩棋时建议 `/dnd on`。

### 6. `/lib search` 没结果

- 确认已经 `/lib open …` 打开了一本书（search 是搜**当前书**正文，不是搜书目）。
- 搜书目请用 `/lib find 关键词`。

---

## 附：管理员填写区（发给小白前请填好）

```text
服务器地址：stdlib.gicp.net
SSH 端口：44681
Linux 用户名：__________
App 安装方式：__________（TestFlight / apk 链接 / 其他）
备注：________________________________
```

填好后提醒小白：

1. 安装 App → 复制公钥发给你登记  
2. 填入上表三项 → 点「连接」
