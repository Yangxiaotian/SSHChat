# 文件收发功能使用指南

English version: [docs/en/file-sharing.md](docs/en/file-sharing.md)

## 用户首次登录看到的信息

当用户通过 SSH 连接到服务器后，会看到：

```
[*] Active room #default. /names /rooms /join /switch /msg /sendfile /leave /part /announce /game /news /dict /clear /help
[*] Rooms: #default
```

**注意**: `/sendfile` 命令已添加到快捷命令列表中，用户一登录就能看到！

## 使用 /help 查看详细说明

用户输入 `/help` 后，会看到完整的命令说明，其中包括：

```
[*] ---------- SSHChat 命令说明 ----------
[*] 普通文字（不以 / 开头）发到「当前活跃房间」，房内在线用户都会收到。
[*]
[*] /join <房间>     加入房间并立刻切到该房；若已在房内则只切换当前房。
[*]              房间名：1～32 字符，仅字母、数字、下划线、连字符。
...（省略其他命令）...
[*]
[*] /sendfile      发送文件到当前房间，你将收到上传网址，密钥另行单独给出。
[*] /sendfile <昵称>    发送文件给指定用户。
[*] /sendfile #<房间>   发送文件到指定房间，成员各自收到不同的下载网址+密钥。
[*]              文件名以你实际上传的文件为准，不必在指令里写。
[*]              密钥不在网址里，打开网页后另行输入；支持图片、视频、PDF等在线预览。
[*]              上传和下载都只能用一次，用过即作废，链接被别人截获也没用。
[*] /help          显示本说明。
```

## 三种用法

| 你想做的事 | 输入 |
| --- | --- |
| 发到当前所在房间 | `/sendfile` |
| 发给某个人 | `/sendfile bob` |
| 发到指定房间 | `/sendfile #dev` |

文件名不用写，以你上传时选的那个文件为准。

## 完整使用流程示例

### 示例 1: 发送文件给特定用户

```
# 1. 用户 alice 发送文件给 bob
alice> /sendfile bob

# alice 收到：
[*] ========== 文件上传信息 ==========
[*] 接收者: bob
[*]
[*] 上传网址:
[*] https://your-domain.com:8443/upload/UoGXVhDKJ42-KqOv...
[*]
[*] 上传密钥: MUJVB2
[*]
[*] 说明:
[*] 1. 打开上传网址，在页面里输入上面的密钥
[*] 2. 选择要发的文件并上传，文件名以所选文件为准
[*] 3. 上传成功即完成，此网址随后作废
[*] 4. 接收者将收到各自的下载网址和密钥，各自只能下载一次
[*] =====================================

# 2. alice 打开网址，看到上传页面
   - 输入密钥: MUJVB2（密钥不在网址里，需要单独输入）
   - 选择文件: document.pdf
   - 点击"开始上传"
   - 看到进度条
   - 上传完成！

# 3. bob 收到通知（无论在线还是离线）：
[*] ========== 收到新文件 ==========
[*] 发件人: alice
[*] 文件名: document.pdf
[*] 大小: 2508.8 KB
[*]
[*] 下载网址:
[*] https://your-domain.com:8443/download/ffh6tr1M5tsW1hi...
[*]
[*] 下载密钥: R9S3PJ
[*]
[*] 说明:
[*] 1. 打开下载网址，在页面里输入上面的密钥
[*] 2. 图片、视频、PDF 等会直接预览，确认后再点按钮保存
[*] 3. 文件只能下载一次，存好之前别关页面
[*] 4. 每个接收者的网址和密钥都不同
[*] ================================

# 4. bob 打开网址，看到下载页面
   - 看到文件信息（名称、大小、类型）
   - 看到"👁️ 在线预览: ✅ 支持"
   - 输入密钥: R9S3PJ
   - 服务器当场生成两条一次性链接：一条用于预览、一条用于下载
   - PDF 自动显示在页面上（走预览那条链接，用完即废）
   - 确认无误后点击"下载文件"按钮保存（走下载那条链接，同样用完即废）
   - 下载完成后整个页面作废，再打开会提示"该下载链接已使用过"
```

### 示例 2: 发送文件到房间

```
# 1. 用户在房间 #dev 中，直接发到当前房间
alice> /sendfile

# 或者指定房间
alice> /sendfile #dev

# alice 收到上传信息（同上）

# 2. 房间 #dev 中的每个成员都收到通知，但每个人的网址和密钥都不同！

# bob 收到:
[*] ========== 收到新文件 ==========
[*] 发件人: alice
[*] 文件名: screenshot.png
[*] 大小: 245.3 KB
[*] 来自房间: #dev
[*]
[*] 下载网址:
[*] https://your-domain.com:8443/download/ABC123...
[*] 下载密钥: XYZ789
[*] ...

# charlie 收到（不同的网址和密钥）:
[*] ========== 收到新文件 ==========
[*] 发件人: alice
[*] 文件名: screenshot.png
[*] 大小: 245.3 KB
[*] 来自房间: #dev
[*]
[*] 下载网址:
[*] https://your-domain.com:8443/download/DEF456...
[*] 下载密钥: UVW012
[*] ...

# 3. bob 和 charlie 打开各自的链接
   - 看到图片信息
   - 输入各自的密钥
   - 图片自动显示在页面上（在线预览）
   - 确认后点击下载按钮保存，各人各自只能下载一次
```

## 支持的文件预览类型

当接收者打开下载链接时，系统会自动检测文件类型：

### ✅ 支持在线预览（21种格式）

**图片** (7种)
- JPG/JPEG - 直接显示
- PNG - 直接显示
- GIF - 支持动画
- WebP - 现代格式
- SVG - 矢量图
- BMP - 位图

**视频** (3种)
- MP4 - HTML5 播放器
- WebM - HTML5 播放器
- OGG - HTML5 播放器

**音频** (3种)
- MP3 - HTML5 播放器
- WAV - HTML5 播放器
- OGG - HTML5 播放器

**文档** (1种)
- PDF - 内嵌查看器

**文本** (7种)
- TXT - 语法高亮
- HTML - 代码显示
- CSS - 代码显示
- JavaScript - 代码显示
- JSON - 格式化显示
- XML - 代码显示
- Markdown - 代码显示

### ❌ 不支持预览的文件类型

对于其他文件类型（如 ZIP, DOC, EXE 等），系统会：
- 显示文件信息
- 提示"❌ 不支持"在线预览
- 输入密钥后直接提供下载按钮

## 安全特性

### 1. 密钥从不出现在网址里
```
聊天里网址和密钥分两行给出
密钥通过网页表单提交（上传走请求头、下载走请求体）
所以密钥不会进入浏览器历史、代理日志或 Referer
即使网址被转发出去，没有密钥也用不了
```

### 2. 预览和下载是两条独立的一次性链接
输入密钥后，服务器当场签发两条互不相同的临时链接：一条预览、一条下载。
两条都**只能用一次**，取完文件立即失效。

```
即使全程走 http 明文、链接被人在半路截获
拿到的也是已经用掉的链接，重放一律 403
```

链接默认 10 分钟内有效（`SSHCHAT_TICKET_TTL_SECONDS` 可调），
重新输入密钥会签发新的一组，同时让上一组立即作废。

### 3. 上传和下载都是一次性
```
上传网址成功上传一次后即作废
下载完成后，该接收者的整个下载页面即作废
```

下载中途断线不会白白损失：只有传输真正完成才会作废，
没传完的话回到页面重新输密钥即可换一组新链接。

确实需要允许重复下载时，把服务端环境变量
`SSHCHAT_ONE_TIME_DOWNLOAD` 设为 `0`（不推荐）。

### 4. 隔离设计
```
房间文件 → 每个成员获得独立网址和密钥
无法通过一个人的凭证访问其他人的下载
```

### 5. 自动过期
```
上传网址 → 60 分钟后自动过期
下载网址 → 24 小时后自动过期
过期文件自动删除
```

## 常见问题

### Q: 为什么指令里不用写文件名了？
A: 文件名从你上传的那个文件直接读取，写了反而容易和实际文件对不上。老写法
`/sendfile bob a.pdf` 仍然能用，多余的文件名会被忽略并给出提示。

### Q: `/sendfile` 不带任何参数会发给谁？
A: 发到你当前所在的房间，房内除你以外的所有人都会各自收到一份下载网址和密钥。

### Q: 如何知道文件是否支持预览？
A: 在下载页面会显示"👁️ 在线预览: ✅ 支持"或"❌ 不支持"

### Q: 我可以多次下载同一个文件吗？
A: 不可以，只能下载一次。预览不算下载，可以先预览确认再保存。真正下载完成后链接立即作废，需要重新拿就联系发送方重发。

### Q: 为什么预览和下载要分成两条链接？
A: 为了让链接被截获也无法复用。输入密钥后服务器签发两条独立的一次性链接，各自取完文件就失效；即使有人在网络中间抓到了链接，拿去重放也只会得到 403。

### Q: 下载到一半断网了，文件是不是就没了？
A: 不会。只有传输真正完成才算用掉；没传完的话回到页面重新输一次密钥，会拿到一组新链接。

### Q: 房间内的其他人能看到我的下载链接吗？
A: 不能。每个人收到的网址和密钥都是独立的，互不相同。

### Q: 如果我输错密钥会怎样？
A: 页面会提示"上传密钥不正确"或"下载密钥不正确"，重新输入正确的密钥即可，链接不会因此失效。

### Q: 文件会永久保存在服务器上吗？
A: 不会。下载链接过期后（默认24小时），文件会被自动删除。

### Q: 最大可以发送多大的文件？
A: 默认限制是 100MB，管理员可以通过 `SSHCHAT_MAX_FILE_SIZE` 环境变量调整。

## 快速参考卡

```
┌────────────────────────────────────────────────┐
│          文件收发功能快速参考                    │
├────────────────────────────────────────────────┤
│                                                │
│  发到当前房间:                                  │
│  /sendfile                                     │
│                                                │
│  发送给用户:                                    │
│  /sendfile <昵称>                              │
│                                                │
│  发送到房间:                                    │
│  /sendfile #<房间>                             │
│                                                │
│  查看帮助:                                      │
│  /help                                         │
│                                                │
├────────────────────────────────────────────────┤
│  特性:                                          │
│  ✓ 不用写文件名                                 │
│  ✓ 密钥从不出现在网址里                          │
│  ✓ 预览/下载各一条一次性链接                     │
│  ✓ 截获也无法重放                                │
│  ✓ 6位随机密钥                                  │
│  ✓ 在线预览 (20+格式)                           │
│  ✓ 精美 Web 界面                                │
│  ✓ 自动 HTTPS                                  │
│  ✓ 每人独立凭证                                 │
└────────────────────────────────────────────────┘
```

## 总结

✅ 用户登录时在欢迎信息中看到 `/sendfile` 命令  
✅ 使用 `/help` 可以查看详细的文件收发说明  
✅ 说明包含了使用方法、在线预览、安全特性等关键信息  
✅ 用户可以轻松了解和使用文件收发功能

---

**更新时间**: 2026-08-08  
**功能版本**: v1.2  
**支持格式**: 21 种文件预览
