# 文件收发功能使用指南

## 用户首次登录看到的信息

当用户通过 SSH 连接到服务器后，会看到：

```
[*] Active room #default. /names /rooms /join /switch /msg /sendfile /leave /part /announce /game /news /dict /clear /help
[*] Rooms: #default
```

**注意**: `/sendfile` 命令已添加到快捷命令列表中，用户一登录就能看到！

## 使用 /help 查看详细说明

用户输入 `/help` 后，会看到完整的命令说明，其中包括：

```
[*] ---------- SSHChat 命令说明 ----------
[*] 普通文字（不以 / 开头）发到「当前活跃房间」，房内在线用户都会收到。
[*]
[*] /join <房间>     加入房间并立刻切到该房；若已在房内则只切换当前房。
[*]              房间名：1～32 字符，仅字母、数字、下划线、连字符。
...（省略其他命令）...
[*]
[*] /sendfile <昵称> <文件名>  发送文件给用户，你将收到一次性上传网址+密钥。
[*] /sendfile #<房间> <文件名>  发送文件到房间，成员各自收到不同的下载网址+密钥。
[*]              打开网址需输入密钥；支持图片、视频、PDF等在线预览；上传/下载后网址立即失效。
[*] /help          显示本说明。
```

## 完整使用流程示例

### 示例 1: 发送文件给特定用户

```
# 1. 用户 alice 发送文件给 bob
alice> /sendfile bob document.pdf

# alice 收到：
[*] ========== 文件上传信息 ==========
[*] 文件名: document.pdf
[*] 接收者: bob
[*]
[*] 上传 URL (一次性):
[*] https://your-domain.com:8443/upload/UoGXVhDKJ42-KqOv...
[*]
[*] 上传密钥: MUJVB2
[*]
[*] 说明:
[*] 1. 访问上传 URL
[*] 2. 输入密钥并上传文件
[*] 3. 上传成功后此 URL 立即作废
[*] 4. 接收者将收到各自的下载 URL 和密钥
[*] =====================================

# 2. alice 打开网址，看到精美的上传页面
   - 输入密钥: MUJVB2
   - 选择文件: document.pdf
   - 点击"开始上传"
   - 看到进度条
   - 上传完成！

# 3. bob 收到通知（无论在线还是离线）：
[*] ========== 收到新文件 ==========
[*] 发件人: alice
[*] 文件名: document.pdf
[*] 大小: 2.45 MB
[*]
[*] 下载 URL (一次性):
[*] https://your-domain.com:8443/download/ffh6tr1M5tsW1hi...
[*]
[*] 下载密钥: R9S3PJ
[*]
[*] 说明:
[*] 1. 访问下载 URL
[*] 2. 输入密钥
[*] 3. 自动预览文件（如支持）
[*] 4. 点击下载按钮获取文件
[*] ================================

# 4. bob 打开网址，看到精美的下载页面
   - 看到文件信息（名称、大小、类型）
   - 看到"👁️ 在线预览: ✅ 支持"
   - 输入密钥: R9S3PJ
   - PDF 自动显示在页面上
   - 可以预览后点击"下载文件"按钮
   - 下载完成，链接失效
```

### 示例 2: 发送文件到房间

```
# 1. 用户在房间 #dev 中
alice> /sendfile #dev screenshot.png

# alice 收到上传信息（同上）

# 2. 房间 #dev 中的每个成员都收到通知，但每个人的 URL 和密钥都不同！

# bob 收到:
[*] ========== 收到新文件 ==========
[*] 发件人: alice
[*] 文件名: screenshot.png
[*] 大小: 245.3 KB
[*] 来自房间: #dev
[*]
[*] 下载 URL (一次性):
[*] https://your-domain.com:8443/download/ABC123...
[*] 下载密钥: XYZ789
[*] ...

# charlie 收到（不同的 URL 和密钥）:
[*] ========== 收到新文件 ==========
[*] 发件人: alice
[*] 文件名: screenshot.png
[*] 大小: 245.3 KB
[*] 来自房间: #dev
[*]
[*] 下载 URL (一次性):
[*] https://your-domain.com:8443/download/DEF456...
[*] 下载密钥: UVW012
[*] ...

# 3. bob 和 charlie 打开各自的链接
   - 看到图片信息
   - 输入各自的密钥
   - 图片自动显示在页面上（在线预览）
   - 可以点击下载按钮保存
   - 各自下载后，各自的链接失效
```

## 支持的文件预览类型

当接收者打开下载链接时，系统会自动检测文件类型：

### ✅ 支持在线预览（21种格式）

**图片** (7种)
- JPG/JPEG - 直接显示
- PNG - 直接显示
- GIF - 支持动画
- WebP - 现代格式
- SVG - 矢量图
- BMP - 位图

**视频** (3种)
- MP4 - HTML5 播放器
- WebM - HTML5 播放器
- OGG - HTML5 播放器

**音频** (3种)
- MP3 - HTML5 播放器
- WAV - HTML5 播放器
- OGG - HTML5 播放器

**文档** (1种)
- PDF - 内嵌查看器

**文本** (7种)
- TXT - 语法高亮
- HTML - 代码显示
- CSS - 代码显示
- JavaScript - 代码显示
- JSON - 格式化显示
- XML - 代码显示
- Markdown - 代码显示

### ❌ 不支持预览的文件类型

对于其他文件类型（如 ZIP, DOC, EXE 等），系统会：
- 显示文件信息
- 提示"❌ 不支持"在线预览
- 输入密钥后直接提供下载按钮

## 安全特性

### 1. 一次性使用
```
上传 URL → 上传一次后立即失效 ✓
下载 URL → 下载一次后立即失效 ✓
```

### 2. 密钥保护
```
所有操作都需要正确的6位密钥
密钥通过私聊传递，不公开
密钥格式：6位大写字母+数字（如 ABC123）
```

### 3. 隔离设计
```
房间文件 → 每个成员获得独立 URL 和密钥
无法通过一个人的凭证访问其他人的下载
```

### 4. 自动过期
```
上传 URL → 60 分钟后自动过期
下载 URL → 24 小时后自动过期
过期文件自动删除
```

## 常见问题

### Q: 如何知道文件是否支持预览？
A: 在下载页面会显示"👁️ 在线预览: ✅ 支持"或"❌ 不支持"

### Q: 我可以多次下载同一个文件吗？
A: 不可以。每个下载 URL 只能使用一次，下载后立即失效。

### Q: 房间内的其他人能看到我的下载链接吗？
A: 不能。每个人收到的 URL 和密钥都是独立的，互不相同。

### Q: 如果我输错密钥会怎样？
A: 系统会提示"Invalid upload key"或"Invalid download key"，需要重新输入正确的密钥。

### Q: 文件会永久保存在服务器上吗？
A: 不会。下载链接过期后（默认24小时），文件会被自动删除。

### Q: 最大可以发送多大的文件？
A: 默认限制是 100MB，管理员可以通过 `SSHCHAT_MAX_FILE_SIZE` 环境变量调整。

## 快速参考卡

```
┌────────────────────────────────────────────────┐
│          文件收发功能快速参考                    │
├────────────────────────────────────────────────┤
│                                                │
│  发送给用户:                                    │
│  /sendfile <昵称> <文件名>                      │
│                                                │
│  发送到房间:                                    │
│  /sendfile #<房间> <文件名>                     │
│                                                │
│  查看帮助:                                      │
│  /help                                         │
│                                                │
├────────────────────────────────────────────────┤
│  特性:                                          │
│  ✓ 一次性 URL                                  │
│  ✓ 6位随机密钥                                  │
│  ✓ 在线预览 (20+格式)                           │
│  ✓ 精美 Web 界面                                │
│  ✓ 自动 HTTPS                                  │
│  ✓ 每人独立凭证                                 │
└────────────────────────────────────────────────┘
```

## 总结

✅ 用户登录时在欢迎信息中看到 `/sendfile` 命令  
✅ 使用 `/help` 可以查看详细的文件收发说明  
✅ 说明包含了使用方法、在线预览、安全特性等关键信息  
✅ 用户可以轻松了解和使用文件收发功能

---

**更新时间**: 2026-08-08  
**功能版本**: v1.0  
**支持格式**: 21 种文件预览
